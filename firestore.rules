rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through server-side Admin SDK — deny direct client access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
